%section.genre
  %header.clearfix
    .videos.row
      %h1= @category.name
  %article.row
    -@category.videos.each do |video|
      .video.col-sm-2
        = link_to image_tag("/tmp/#{video.small_cover_url}.jpg"), video_path(video)